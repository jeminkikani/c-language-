// with arg no return
#include<stdio.h>

int mul(int a, int b)
{
     int total;
     total = a*b;
     printf("multipal both of a and b :- %d",total);
}
   
void main()
    {
        mul(10,2);
    /*   int x,y;
       printf("enter x :- ");
       scanf("%d",&x);
       printf("enter y :- ");
       scanf("%d",&y);
       mul(x,y); 
    */
    }
